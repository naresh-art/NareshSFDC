public class caluculatorlight {
@Auraenabled
    public static decimal caluculateValues(decimal fstnumber,decimal secnumber)
    {
       decimal sum = fstnumber + secnumber;
       return sum;
      //  return(fstnumber + secnumber);
    }
}
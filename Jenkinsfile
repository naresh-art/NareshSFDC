pipeline {
    agent any 
    stages {
        stage('Clone the repo') {
            steps {
                echo 'clone the repo'
                sh 'git clone https://github.com/naresh-art/NareshSFDC.git'
            }
        }
    }
}
